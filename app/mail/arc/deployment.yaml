apiVersion: apps/v1
kind: Deployment
metadata:
  name: filter
  annotations:
    reloader.stakater.com/auto: "true"
spec:
  # TODO: scale to 3
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  template:
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchLabels: {}
            topologyKey: kubernetes.io/hostname
      containers:
        - name: openarc
          image: ghcr.io/teapotovh/openarc:v0.0.10
          ports:
            - name: milter
              containerPort: 8893
          volumeMounts:
            - name: config
              mountPath: /etc/openarc.conf
              subPath: openarc.conf
              readOnly: true
            - name: keys
              mountPath: /etc/openarc/keys
              readOnly: true
          resources:
            requests:
              cpu: 5m
              memory: 16Mi
            limits:
              cpu: 50m
              memory: 64Mi
      volumes:
        - name: config
          configMap:
            name: config
        - name: keys
          secret:
            secretName: mail-arc-keys

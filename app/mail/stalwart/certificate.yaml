apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: certificate
spec:
  secretName: mail-stalwart-certificate
  dnsNames:
    - imap.teapot.ovh
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer

apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: app-gateway
spec:
  interval: 1h
  path: app/gateway
  prune: true
  dependsOn:
    - name: app
    - name: gateway-api
    - name: envoy-gateway-crd
  sourceRef:
    kind: GitRepository
    name: prod

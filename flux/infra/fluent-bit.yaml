apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: fluent-bit
spec:
  interval: 1h
  path: infra/fluent-bit
  prune: true
  sourceRef:
    kind: GitRepository
    name: prod

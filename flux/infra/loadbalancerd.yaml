apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: loadbalancerd
spec:
  interval: 1h
  path: infra/loadbalancerd
  prune: true
  sourceRef:
    kind: GitRepository
    name: prod
